export interface User {
  username: string;
  token: string;
}

// Another way to define the User type using a type alias
// This is a more concise way to define the same structure as the User interface above
// export type User1= {
//     username: string;
//     token: string;
//   }
